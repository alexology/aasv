% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align_species_seq.R
\name{align_species_seq}
\alias{align_species_seq}
\title{Download and align species-level sequences}
\usage{
align_species_seq(
  taxonomy = NULL,
  alignment_dir = NULL,
  overwrite = FALSE,
  timeout = 7200,
  min_length = 500,
  max_length = 1000,
  genetic_code = "5",
  ...
)
}
\arguments{
\item{taxonomy}{A file with the taxonomy assigned to ASV. It must have at least
3 columns: family, genus and species.}

\item{alignment_dir}{Path to a folder to store the results.}

\item{overwrite}{Overwrite existing data.}

\item{timeout}{Download timeout in seconds passed to \code{options(timeout)}.}

\item{min_length}{Select only the downloaded sequences longer than this value.}

\item{max_length}{Select only the downloaded sequences shorter than this value.}

\item{genetic_code}{Genetic code. Default to invertebrate mitochondrial code.}

\item{...}{Further arguments to be passed to \code{AlignSeqs} of the \code{DECIPHER} package.}
}
\value{
Called for its side effects (downloading raw sequences and writing
  aligned FASTA files to disk). Returns \code{NULL} invisibly.
}
\description{
This function downloads and aligns sequences from species within the same family
of the query sequences.
}
\details{
This function downloads sequences from BOLD to build a reference database that is used for
further calculations. Given a set of query species, \code{align_species_seq} downloads
all the available sequences up to family level. Only the sequences with a full
species name are retained (e.g. Gorilla sp. is removed). Once the sequences are
downloaded and filtered, they are aligned for each species separately with
the function \code{AlignSeqs} of the package \code{DECIPHER}. Raw sequences are
saved to disk in a folder called \code{raw_sequences}, while the aligned
sequences are saved in a folder called \code{alignments}.
}
\examples{
\dontrun{
align_species_seq(
  data.frame(family  = "Oligoneuridae",
             genus   = "Oligoneuriella",
             species = "Oligoneuriella rhenana"),
  min_length  = 640,
  max_length  = 700,
  terminalGap = -1
)
}
}
